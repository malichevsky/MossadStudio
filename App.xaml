<Application x:Class="MossadStudio.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Bootstrapper.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="ScrollBarThumb" Color="#444444" />
        <SolidColorBrush x:Key="ScrollBarBackground" Color="#181818" />
        
        <!-- Overriding WPF's default system Control color to fix the white box tucked in the ScrollViewer corner -->
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#181818" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightBrushKey}" Color="#181818" />

        <ControlTemplate x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid Background="{StaticResource ScrollBarBackground}">
                <Track x:Name="PART_Track" IsDirectionReversed="true">
                    <Track.Thumb>
                        <Thumb>
                            <Thumb.Template>
                                <ControlTemplate TargetType="Thumb">
                                    <Border Background="{StaticResource ScrollBarThumb}" CornerRadius="2" Margin="1,0,1,0" Width="5" HorizontalAlignment="Center" />
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Track.Thumb>
                </Track>
            </Grid>
        </ControlTemplate>
        
        <ControlTemplate x:Key="HorizontalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid Background="{StaticResource ScrollBarBackground}">
                <Track x:Name="PART_Track" IsDirectionReversed="False">
                    <Track.Thumb>
                        <Thumb>
                            <Thumb.Template>
                                <ControlTemplate TargetType="Thumb">
                                    <Border Background="{StaticResource ScrollBarThumb}" CornerRadius="2" Margin="0,1,0,1" Height="5" VerticalAlignment="Center" />
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Track.Thumb>
                </Track>
            </Grid>
        </ControlTemplate>
        
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{StaticResource ScrollBarBackground}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Template" Value="{StaticResource VerticalScrollBar}"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="10" />
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
